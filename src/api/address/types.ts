// TODO: Add the type after the API is fixed
export type GetAddressResponseType = unknown;

export type CreateAddressRequestType = {
  city: string;
  country: string;
  street: string;
  houseNumber: string;
  latitude: string;
  longitude: string;
  email: string;
};

// TODO: Add the type after the API is fixed
export type CreateAddressResponseType = unknown;
